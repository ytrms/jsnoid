{
  "name": "jsnoid",
  "version": "0.0.1",
  "description": "Arkanoid clone made in Phaser",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel serve src/index.html",
    "build": "parcel build src/index.html --public-url ./ --no-source-maps",
    "clean": "rm -rv dist/*"
  },
  "author": "Lorenzo Gravina",
  "license": "ISC",
  "dependencies": {
    "phaser": "^3.55.2"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.5"
  }
}